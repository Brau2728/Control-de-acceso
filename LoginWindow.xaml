<Window x:Class="prueba1.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Autenticación SICB-GAN" Height="450" Width="350"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Border CornerRadius="20" Background="White" BorderBrush="#00204A" BorderThickness="2">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

            <Ellipse Grid.Row="0" Width="80" Height="80" Fill="#00204A" HorizontalAlignment="Center">
                </Ellipse>

            <TextBlock Grid.Row="1" Text="ACCESO RESTRINGIDO" HorizontalAlignment="Center" 
                       Margin="0,20" FontWeight="Black" Foreground="#00204A" FontSize="18"/>

            <StackPanel Grid.Row="2" VerticalAlignment="Center">
                <TextBlock Text="USUARIO / MATRÍCULA" Foreground="Gray" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="txtUser" Height="35" Margin="0,0,0,20" VerticalContentAlignment="Center" Padding="5"/>

                <TextBlock Text="CONTRASEÑA" Foreground="Gray" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPass" Height="35" VerticalContentAlignment="Center" Padding="5"/>
            </StackPanel>

            <StackPanel Grid.Row="3">
                <Button Content="ENTRAR" Background="#00204A" Foreground="White" Height="40" FontWeight="Bold" Click="Login_Click"/>
                <Button Content="CANCELAR" Background="Transparent" Foreground="Gray" BorderThickness="0" Margin="0,10" Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>