<Window x:Class="prueba1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:prueba1"
        mc:Ignorable="d"
        Title="SICB-GAN SEMAR - Control de Acceso" 
        WindowState="Maximized" 
        WindowStyle="None"
        Background="#0F172A">
    <!-- Fondo Slate 900 -->

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <!-- 1. DEFINICIONES DE FILAS (Deben ir al principio) -->
        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>

        <!-- 2. RECURSOS DE ESTILO -->
        <Grid.Resources>
            <!-- Estilo para redondear el botón de Login -->
            <Style x:Key="LoginButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#334155"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="27">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Estilo para redondear el botón de Simulación -->
            <Style x:Key="SimulateButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#D4AF37"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Padding" Value="20,10"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <!-- 3. CONTENIDO (Elementos hijos) -->

        <!-- ESCUDO DE FONDO (Watermark) -->
        <Grid x:Name="BackgroundShield" Grid.RowSpan="3" Opacity="0.04" IsHitTestVisible="False">
            <Viewbox Stretch="Uniform" Margin="250">
                <Canvas Width="200" Height="250">
                    <Path Fill="White" Data="M100,0 L10,30 L10,120 C10,180 100,250 100,250 C100,250 190,180 190,120 L190,30 L100,0 Z M100,40 L160,60 L160,115 C160,160 100,210 100,210 C100,210 40,160 40,115 L40,60 L100,40 Z"/>
                    <Path Fill="White" Data="M90,70 L110,70 L110,140 L130,140 L130,150 L70,150 L70,140 L90,140 Z"/>
                </Canvas>
            </Viewbox>
        </Grid>

        <!-- CABECERA INSTITUCIONAL -->
        <Border Grid.Row="0" Background="#1E293B" BorderBrush="#D4AF37" BorderThickness="0,0,0,3">
            <Grid Margin="30,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="SECRETARÍA DE MARINA" Foreground="White" FontSize="24" FontWeight="Black"/>
                    <TextBlock Text="DÉCIMA SEXTA ZONA NAVAL" Foreground="#D4AF37" FontSize="12" FontWeight="Bold" Opacity="0.8"/>
                </StackPanel>

                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal">
                    <StackPanel Margin="0,0,25,0" VerticalAlignment="Center">
                        <TextBlock Text="SISTEMA INTEGRAL DE CONTROL BIOMÉTRICO" Foreground="#94A3B8" FontSize="11" HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding FechaHoraActual}" Foreground="White" FontSize="24" FontWeight="Light" HorizontalAlignment="Right"/>
                    </StackPanel>

                    <Button Width="55" Height="55" Style="{StaticResource LoginButtonStyle}" Click="OpenLogin_Click">
                        <StackPanel>
                            <TextBlock Text="👤" FontSize="20" Foreground="#D4AF37" HorizontalAlignment="Center"/>
                            <TextBlock Text="LOGIN" FontSize="8" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- BOTÓN DE CERRAR VENTANA -->
        <Button Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top" 
                Width="40" Height="40" Background="Transparent" BorderThickness="0"
                Foreground="White" Click="CloseWindow_Click" Margin="0,5,5,0" Opacity="0.5">
            <TextBlock Text="✕" FontSize="20" FontWeight="Light"/>
        </Button>

        <!-- ÁREA PRINCIPAL DINÁMICA -->
        <Grid Grid.Row="1">
            <!-- CAPA 1: ESTADO DE ESPERA -->
            <StackPanel x:Name="CapaEspera" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding MarinoActual}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <Border Width="180" Height="180" CornerRadius="90" Background="#1E293B" BorderBrush="#334155" BorderThickness="4">
                    <TextBlock Text="⚓" FontSize="80" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#D4AF37" Opacity="0.8"/>
                </Border>
                <TextBlock Text="ESPERANDO HUELLA" FontSize="42" FontWeight="Black" Foreground="White" Margin="0,30,0,0" HorizontalAlignment="Center"/>
                <TextBlock Text="POR FAVOR, COLOQUE SU DEDO EN EL LECTOR" FontSize="16" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,10,0,0"/>

                <Button Style="{StaticResource SimulateButtonStyle}" Click="Button_Click" Margin="0,40,0,0">
                    <TextBlock Text="SIMULAR ESCANEO" Foreground="#00204A" FontWeight="Bold"/>
                </Button>
            </StackPanel>

            <!-- CAPA 2: RESULTADO DE ACCESO -->
            <Border x:Name="CapaResultado" Background="Transparent">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding MarinoActual}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Grid>
                    <Border Background="{Binding StatusColor}" Opacity="0.95"/>
                    <Grid Margin="60">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="400"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" CornerRadius="25" Background="#111" BorderBrush="White" BorderThickness="10" VerticalAlignment="Center" Height="550">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.5"/>
                            </Border.Effect>
                            <Grid>
                                <Image Source="{Binding MarinoActual.FotoPath}" Stretch="UniformToFill">
                                    <Image.Clip>
                                        <RectangleGeometry Rect="0,0,380,530" RadiusX="15" RadiusY="15"/>
                                    </Image.Clip>
                                </Image>
                                <Border VerticalAlignment="Bottom" Background="#CC000000" Padding="15">
                                    <TextBlock Text="{Binding MarinoActual.Matricula}" Foreground="#D4AF37" FontSize="24" FontWeight="Black" HorizontalAlignment="Center" FontFamily="Consolas"/>
                                </Border>
                            </Grid>
                        </Border>

                        <StackPanel Grid.Column="1" Margin="60,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding StatusMensaje}" Foreground="White" FontSize="100" FontWeight="Black" Margin="0,0,0,20"/>
                            <Border Background="White" Opacity="0.1" Height="2" Margin="0,0,0,40"/>
                            <TextBlock Text="GRADO" Foreground="White" FontSize="16" FontWeight="Bold" Opacity="0.7"/>
                            <TextBlock Text="{Binding MarinoActual.Grado}" FontSize="54" FontWeight="Light" Foreground="White" Margin="0,0,0,30"/>
                            <TextBlock Text="NOMBRE COMPLETO" Foreground="White" FontSize="16" FontWeight="Bold" Opacity="0.7"/>
                            <TextBlock Text="{Binding MarinoActual.NombreCompleto}" FontSize="42" FontWeight="Black" Foreground="White" TextWrapping="Wrap" Margin="0,0,0,30"/>
                            <StackPanel>
                                <TextBlock Text="JEFATURA / ÁREA" Foreground="White" FontSize="16" FontWeight="Bold" Opacity="0.7"/>
                                <TextBlock Text="{Binding MarinoActual.Jefatura}" FontSize="28" Foreground="White" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- PIE DE PÁGINA -->
        <Border Grid.Row="2" Background="#020617" BorderBrush="#1E293B" BorderThickness="0,1,0,0">
            <TextBlock Text="SISTEMA DE USO OFICIAL EXCLUSIVO PARA LA SECRETARÍA DE MARINA - MÉXICO" 
                       Foreground="#475569" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" FontWeight="Bold"/>
        </Border>
    </Grid>
</Window>